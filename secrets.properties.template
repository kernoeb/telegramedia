# Copy this file to `secrets.properties` (gitignored) and fill in your Telegram API
# credentials from https://my.telegram.org → API development tools.
# Without these, the app runs in demo mode against the in-memory FakeTelegramService.
TELEGRAM_API_ID=0
TELEGRAM_API_HASH=
